import ProfileClient from './ProfileClient';

// 就活版（career）基本情報入力ページ。
// 受験版 app/input/basic/page.tsx を 100% 複製した土台（Phase1）。
// 実体は ProfileClient（'use client'）側に持つ。
export default function CareerProfilePage() {
  return <ProfileClient />;
}
